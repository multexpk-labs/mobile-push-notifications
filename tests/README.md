# Tests

Recommended coverage:
- PHP payload validation
- JavaScript payload normalization
- Python fixture validation
- token lifecycle state transitions
- provider adapter selection
- retry classification
- authorization and preferences
- deep-link validation

Public CI should not require production FCM/APNs credentials or real devices.
