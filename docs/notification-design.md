# Notification Design

Separate notification content from transport.

A useful notification can include title, body, data, category/type, priority, and an application-specific destination.

Keep payloads small and validate data before delivery.
