# Security

Protect device tokens, user identifiers, notification history, provider credentials, and deep-link destinations.

Use authentication, authorization, rate limits, secret management, least privilege, and logs that avoid sensitive payloads.
